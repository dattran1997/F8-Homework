import React from 'react';
import App from './App';

console.log('check ib load', React);

const domNode = document.getElementById('root');
// const root = createRoot(domNode);

ReactDOM.render(<App />);